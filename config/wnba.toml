[sport]
name = "wnba"
display_name = "WNBA"

[prizepicks]
leagues = { wnba = 3 }

[stat_types]
single = ["PTS", "REB", "AST", "STL", "BLK", "TOV", "FG3M", "PRA"]
combined = ["PR", "PA", "PRA", "FPTS"]

[features]
rolling_windows = [3, 5, 10]
season_lookback = 3
min_games_for_features = 4
recency_decay = 0.001

[model]
max_line_diff = 4.0
max_edge_override = 0.15
